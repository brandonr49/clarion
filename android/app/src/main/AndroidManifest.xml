<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Clarion">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget input activities (launched by widgets) -->
        <activity
            android:name=".widget.WidgetInputActivity"
            android:exported="false"
            android:theme="@style/Theme.Clarion"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".widget.WidgetQueryActivity"
            android:exported="false"
            android:theme="@style/Theme.Clarion"
            android:windowSoftInputMode="adjustResize" />

        <!-- Quick Note Widget -->
        <receiver
            android:name=".widget.QuickNoteWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_note_widget_info" />
        </receiver>

        <!-- Query Dashboard Widget -->
        <receiver
            android:name=".widget.QueryWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/query_widget_info" />
        </receiver>
    </application>
</manifest>
